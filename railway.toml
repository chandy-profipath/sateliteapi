[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn satelite:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
