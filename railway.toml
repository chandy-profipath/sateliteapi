[build]
builder = "nixpacks"

[deploy]
startCommand = "/opt/venv/bin/uvicorn satelite:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
