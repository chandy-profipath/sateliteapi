providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "gdal", "geos", "proj", "gcc"]

[phases.install]
cmds = ["python -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn satelite:app --host 0.0.0.0 --port $PORT"
