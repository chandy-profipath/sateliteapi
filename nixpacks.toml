providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311.pkgs.pip", "gdal", "geos", "proj", "gcc"]

[phases.install]
cmds = ["python -m ensurepip", "python -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn satelite:app --host 0.0.0.0 --port $PORT"
